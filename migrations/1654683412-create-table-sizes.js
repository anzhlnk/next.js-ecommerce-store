exports.up = async (sql) => {
  await sql`CREATE TABLE sizes (
  id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  size varchar(50) NOT NULL
  )`;
};

exports.down = async (sql) => {
  await sql`DROP TABLE sizes`;
};
